name: 🚀 Deploy Website to FTP

on:
  push:
    branches:
      - master # o main, según la rama por defecto

jobs:
  web-deploy:
    name: 🚚 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files to FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.ingelyv.cl
        username: deploy@ingelyv.cl
        password: ${{ secrets.FTP_PASSWORD }}
        # Si deseas excluir la carpeta .git y archivos de config:
        exclude: |
          **/.git*
          **/.git*/**
          **/deploy.ps1
          **/.agents/**
          **/README.md
