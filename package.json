{
  "name": "webingelyv",
  "private": true,
  "version": "1.0.0",
  "description": "Sitio corporativo INGELYV SPA",
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.css -o css/tailwind.css --minify",
    "watch:css": "tailwindcss -i src/tailwind.css -o css/tailwind.css --watch",
    "build": "npm run build:css && bash scripts/build-dist.sh"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.9",
    "tailwindcss": "^3.4.17"
  }
}
